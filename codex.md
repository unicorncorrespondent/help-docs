# Codex 对话记录

> 说明：以下为本次协作会话的按时间顺序整理记录（用户请求与助手执行结果）。

## 1. 文档内容改写与结构统一（E/C/D/F 多模块）

1. 用户：将概述链接改为可直接点击跳转对应页面。
   助手：完成对应链接结构调整。

2. 用户：按其他概述模板修改 `E-02-01`，改动处注释保留、插入新内容。
   助手：按模板完成改写并保留旧内容注释。

3. 用户：补充“RNA二级结构重要性”和“现有技术不足”，引出 LinearFold。
   助手：补充背景段并强化模型引入逻辑。

4. 用户：比较 LinearFold 与 LinearPartition，判断是否可合并模块。
   助手：给出差异分析与不建议直接合并结论（功能定位不同）。

5. 用户：按输入页模板修改 `E-02-02`，注释保留旧内容。
   助手：完成。

6. 用户：不要参考 `E-02` 之后未修改文档。
   助手：按该约束继续编辑。

7. 用户：按结果展示/数据解读风格修改 `E-02-03`（提供参考内容）。
   助手：完成并保持风格一致。

8. 用户：修改 `E-03-01` 与 `E-04-01` 概述（同规则）。
   助手：完成。

9. 用户：记住概述模块要求。
   助手：后续持续遵守。

10. 用户：LinearFold 概览加指向 LinearPartition 概览的引导。
    助手：完成互链与场景说明。

11. 用户：继续修改 `E-03-02`、`E-04-02` 输入页。
    助手：完成。

12. 用户：继续修改 `E-03-03` 结果解读页；再修改 `E-04`、`E-05` 部分。
    助手：按既定规则完成对应页面更新。

13. 用户：修改 `E-05-06 FAQ`（参照其他 FAQ）。
    助手：完成。

14. 用户：根据给定内容修改 `E-05-04`。
    助手：完成结果页重写与示意图说明。

15. 用户：删除所有 Pricing 页面的 TLDR、`>` 注释和多余 `---`。
    助手：批量清理完成。

## 2. GitHub Pages 部署与仓库操作

16. 用户：阅读 `run-docs.sh`，将项目部署到 GitHub 可网页访问。
    助手：完成仓库初始化、远程配置、推送与部署流程设置。

17. 用户：要求直接执行 git 操作，并提供远程仓库 `git@github.com:unicorncorrespondent/help-docs.git`。
    助手：完成 remote 设置、push、触发 workflow。

18. 用户：检查部署状态（附截图），并询问如何不私有。
    助手：指导仓库可见性与 Pages 配置；继续排查 deploy 失败原因。

19. 用户：继续重试部署、询问访问地址。
    助手：提供站点地址并持续协助检查 Actions/Pages 状态。

20. 用户：删除所有“上一步/下一步”内容（UI 已有）。
    助手：全量删除并复查。

21. 用户：发现未链接 `C-03-04`，要求修复但不推送。
    助手：本地修复链接，不推送。

## 3. 侧边栏与导航重构

22. 用户：提出侧边栏两项改造：
    - 母菜单直接作为主页链接（不单列“主页”子项）
    - `HelixDesign-Binder/Antibody` 改中文名称
    助手：完成 `sidebars.ts` 重构并本地构建通过。

23. 用户：要求本地预览；并清理占用 3000 端口进程。
    助手：协助处理端口与预览启动。

24. 用户：确认是否仍有未删除分割线。
    助手：检查并清理。

25. 用户：将“蛋白质与小分子”改为“蛋白质/多肽药物研发”。
    助手：完成。

26. 用户：将 `HelixADMET` 改为“小分子 ADMET 性质预测”。
    助手：完成。

27. 用户：将 `MMGBSA` 改为“结合自由能计算”。
    助手：完成。

28. 用户：修改 mRNA 工具区 4 个母菜单标签：
    - LinearFold -> 二级结构预测
    - LinearPartition -> 配对概率分布预测
    - HelixUTRDesign -> UTR设计
    - LinearDesign -> mRNA 序列设计
    助手：完成。

29. 用户：删除所有 `//` 注释片段。
    助手：先扫描待删列表；用户中途叫停。

30. 用户：要求先列出“已删除部分”。
    助手：确认未实际执行删除，仅提供待删清单。

31. 用户：改为“整理变化并 commit + 重新部署”。
    助手：先构建检查，后按用户中途指令暂停。

32. 用户：在提交前再改两处标签：
    - mRNA 工具 -> mRNA 药物研发
    - 小分子药物 -> 小分子药物研发
    助手：完成。

33. 用户：直接 `commit + push`。
    助手：完成提交与推送，触发部署。

## 4. C-01-02.5 / C-01-02.6 / C-01-02.7 内链建设

34. 用户：`C-01-02.5` 有外链，要求改为内链；并根据提供内容新建内部文档。
    助手：
    - 新建 `C-01-02.6-JSONInputOutput.md`（`files` 与 `docs`）
    - 将 `C-01-02.5` 外链改为指向内部文档
    - 同步并校验。

35. 用户：询问“为什么多了很多重复页面”。
    助手：定位为 `docs` 目录存在未跟踪 `* 2.md` 异常副本；执行 `sync-docs.sh` 清理恢复。

36. 用户：要求提升 `C-01-02.6` 易读性。
    助手：重构文档结构（快速导航、快速上手、字段速查、分层字段解释、常见错误排查），并同步。

37. 用户：`C-01-02.6` 中仍有外链，要求同样处理并给出内容。
    助手：
    - 新建 `C-01-02.7-APISDKGuide.md`（`files` 与 `docs`）
    - 将 `C-01-02.6` 外链改为内链到 `C-01-02.7`
    - 同步并校验。

38. 用户：确认后要求全部提交（包含用户自行修改部分）。
    助手：完成 `git add .`、commit 与 push。

## 5. 本次关键提交

- `97dc01e` — `docs: update navigation labels and sync documentation content`
- `5dd51c3` — `docs: add internal JSON/API guides and sync content updates`

## 6. 当前结果摘要

- 侧边栏中文化与母菜单主页化已完成。
- C-01-02.5 外链已替换为内部文档。
- 新增：
  - `C-01-02.6-JSONInputOutput.md`
  - `C-01-02.7-APISDKGuide.md`
- 重复 `* 2.md` 页面已清理。
- 最新改动已推送，触发 GitHub Pages 重新部署。
